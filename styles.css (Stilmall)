/* Grundläggande stil */
body {
    margin: 0;
    padding: 0;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: Arial, sans-serif;
    overflow: hidden;
    transition: background 0.5s ease; /* Smooth transition for background change */
}

/* Container för knappen */
.container {
    text-align: center;
}

/* Bakgrund för Light Mode */
.light-mode {
    background: linear-gradient(to bottom, #a0a0f2, #ffb6c1);
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
}

.light-mode .stars {
    position: absolute;
    top: 10%;
    left: 0;
    right: 0;
    height: 100%;
    width: 100%;
}

.star {
    position: absolute;
    background-color: white;
    border-radius: 50%;
    opacity: 0.8;
    animation: starAnimation 3s infinite;
}

/* Bakgrund för Dark Mode */
.dark-mode {
    background: linear-gradient(to bottom, #000000, #5a2a8a);
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
}

.dark-mode .stars {
    position: absolute;
    top: 10%;
    left: 0;
    right: 0;
    height: 100%;
    width: 100%;
}

@keyframes starAnimation {
    0% {
        transform: translate(0, 0);
        opacity: 1;
    }
    50% {
        transform: translate(10px, 10px);
        opacity: 0.5;
    }
    100% {
        transform: translate(0, 0);
        opacity: 1;
    }
}

/* Stil för knappen */
.change-background-btn {
    position: absolute;
    top: 10px;
    right: 10px;
    padding: 10px 20px;
    background-color: #fff;
    border: none;
    cursor: pointer;
    z-index: 1;
    border-radius: 5px;
    font-size: 16px;
    font-weight: bold;
    transition: background-color 0.3s;
}

.change-background-btn:hover {
    background-color: #f0f0f0;
}
