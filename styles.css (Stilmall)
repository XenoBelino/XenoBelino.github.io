/* Grundinställningar */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body, html {
    width: 100%;
    height: 100%;
    font-family: Arial, sans-serif;
}

/* Dynamisk bakgrund */
.background {
    position: relative;
    width: 100%;
    height: 100%;
    background: linear-gradient(to bottom, #e0a1e0, #f5c0c0); /* Ljus bakgrund */
    transition: background 1s ease-in-out;
}

.dark-mode .background {
    background: linear-gradient(to bottom, #2a2a2a, #4b1e6b); /* Mörk bakgrund */
}

/* Stjärnor */
.stars {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: url('star-pattern.png') repeat;
    pointer-events: none; /* Så att stjärnorna inte blockerar användarinteraktion */
    animation: twinkle 2s infinite;
}

@keyframes twinkle {
    0% { opacity: 0.3; }
    50% { opacity: 1; }
    100% { opacity: 0.3; }
}

/* Moln (simulerat med vit färg) */
.clouds {
    position: absolute;
    top: 10%;
    width: 100%;
    height: 20%;
    background: linear-gradient(to bottom, rgba(255, 255, 255, 0.3), rgba(255, 255, 255, 0));
}

/* Innehåll */
.content {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    color: #fff;
}

#go-to-page-btn {
    display: inline-block;
    padding: 10px 20px;
    background-color: #ff4c6a;
    color: white;
    text-decoration: none;
    border-radius: 5px;
    margin-top: 20px;
}

#change-background-btn {
    position: fixed;
    top: 10px;
    right: 10px;
    padding: 10px 20px;
    background-color: #ff4c6a;
    color: white;
    border: none;
    cursor: pointer;
}

/* Förändring av knappar */
button:hover {
    background-color: #ff2a3b;
}
